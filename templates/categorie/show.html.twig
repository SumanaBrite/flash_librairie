{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
    <h1>Catégorie</h1>
    <br></br>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie.libelle }}</td>
            </tr>
        </tbody>
    </table>
    <br></br>

    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}" class="btn btn-secondary btn-lg">Editer</a>
    <br></br>

    <a href="{{ path('categorie_index') }}" class="btn btn-secondary btn-lg">Retour à la liste</a>

    
    <br></br>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
